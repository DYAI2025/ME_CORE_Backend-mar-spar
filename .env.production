# Production Environment Configuration for Fly.io
# This file shows the minimal required environment variables for deployment

# Required: MongoDB connection (use MongoDB Atlas)
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/marker_engine?retryWrites=true&w=majority

# API Configuration (Fly.io automatically sets PORT)
API_HOST=0.0.0.0
ENVIRONMENT=production

# Resources path (automatically created in Docker)
DETECTOR_PATH=/app/resources

# Performance settings
CACHE_TYPE=memory
CACHE_DEFAULT_TTL=3600
ENABLE_METRICS=false
SPARK_NLP_ENABLED=false

# Optional: API Keys for LLM features (can be added later)
# MOONSHOT_API_KEY=your_moonshot_api_key
# OPENAI_API_KEY=your_openai_api_key

# Optional: Notification webhook
# NOTIFICATION_WEBHOOK_URL=https://your-webhook-url.com/notify